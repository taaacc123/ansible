---
- hosts: dev
  become: true
  tasks:
    - apt:
        name:
          - nginx
        state: present
    - service: name=nginx state=started enabled=yes
    - raw: "echo Welcome to 28minutes, this is $(hostname) | sudo tee /var/www/html/index.html" 