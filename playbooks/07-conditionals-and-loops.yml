---
- hosts: qa
  vars: 
    system: "Windows"
    color: "Red"
  tasks:
#    - debug: var=hostvars
    - debug: var=ansible_system
    - debug: var=color
      when: system == 'Linux'
#Prints Red if system is Linux